import pandas as pd

# Esto iría dentro de la sección de Hipótesis o en una nueva sección
st.subheader("📁 Cargar datos desde CSV")
archivo_subido = st.file_uploader("Sube tu archivo CSV", type=["csv"])

if archivo_subido is not None:
    df = pd.read_csv(archivo_subido)
    st.write("Vista previa de los datos:")
    st.dataframe(df.head()) # Muestra las primeras filas
    
    # Supongamos que el usuario elige una columna numérica
    columna = st.selectbox("Selecciona la columna para analizar", df.columns)
    
    # Calculamos los valores automáticamente para la prueba
    x_barra = df[columna].mean()
    sigma_s = df[columna].std()
    n_muestra = len(df[columna])
    
    st.success(f"Datos calculados: Media={x_barra:.2f}, Desviación={sigma_s:.2f}, n={n_muestra}")